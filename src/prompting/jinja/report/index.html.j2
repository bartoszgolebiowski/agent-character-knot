<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ report_title }}</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <header>
        <h1>{{ report_title }}</h1>
        <p class="subtitle">Analysis of "{{ book_title }}"</p>
    </header>

    <nav>
        <a href="index.html">Home</a>
        <a href="#characters">Characters ({{ characters | length }})</a>
        <a href="#chapters">Chapters ({{ chapters | length }})</a>
    </nav>

    <main>
        <section>
            <h2>Summary</h2>
            <p>This report contains analysis of <strong>{{ characters | length }}</strong> characters,
            <strong>{{ total_relationships }}</strong> relationship connections, and
            <strong>{{ total_events }}</strong> significant events across
            <strong>{{ chapters | length }}</strong> chapters.</p>
        </section>

        <section id="characters">
            <h2>Characters</h2>
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Importance</th>
                        <th>Also Known As</th>
                        <th>First Appearance</th>
                    </tr>
                </thead>
                <tbody>
                    {% for character in characters %}
                    <tr>
                        <td>{{ character.id | character_link | safe }}</td>
                        <td class="importance-score">{{ character.importance_score | importance_stars }}</td>
                        <td class="aliases">{{ character.aliases | join(", ") if character.aliases else "-" }}</td>
                        <td>{{ character.first_appearance_chapter | chapter_link | safe }}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </section>

        <section id="chapters">
            <h2>Chapters</h2>
            <table>
                <thead>
                    <tr>
                        <th>Chapter</th>
                        <th>Characters</th>
                        <th>Events</th>
                        <th>Relationships</th>
                    </tr>
                </thead>
                <tbody>
                    {% for chapter in chapters %}
                    <tr>
                        <td>{{ chapter.index | chapter_link(chapter.title) | safe }}</td>
                        <td>{{ chapter.characters_introduced | length }} new</td>
                        <td>{{ chapter.events_count }}</td>
                        <td>{{ chapter.relationships_count }}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </section>
    </main>

    <footer>
        <p>Generated by StoryGraph AI Agent</p>
    </footer>
</body>
</html>
